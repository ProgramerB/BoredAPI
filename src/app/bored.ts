export interface bored{
    activity: string,
    accessibility: number,
    type: string,
    participants: number,
    price: number,
    link: string,
    key: number
}

export interface boreds{
    bored: bored[]
}